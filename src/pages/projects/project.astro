---
import BaseLayout from "../../layouts/BaseLayout.astro";
import ProjectHero from "../../components/ProjectHero.astro";
import ProjectSection from "../../components/ProjectSection.astro";

import { loadImages } from "../../util/loadImages";

// Data

import type { ACTION_ERROR_CODES } from "astro:actions";

const projects = import.meta.glob("../../data/projects/**/*.json");

// Import all images
const imageModules = import.meta.glob(
    "../../data/projects/**/*.{jpg,jpeg,gif,png}",
);

const imageMap = await loadImages(imageModules);

console.log(projects);
---

<!-- <BaseLayout title={project.title}>
    
    <ProjectHero hero={project.hero} cover={imageMap[project.hero.bgImg]} />

    {project.sections.map((section) => <ProjectSection {...section} />)}
</BaseLayout> -->
