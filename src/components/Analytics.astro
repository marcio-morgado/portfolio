---

---

<!-- Google tag (gtag.js)  -->
<script type="text/partytown">
    window.dataLayer = window.dataLayer || [];
    function gtag() {
        dataLayer.push(arguments);
    }
    gtag("js", new Date());

    gtag("config", "G-5PKGBVZYEJ", {
        anonymize_ip: true,
        send_page_view: true,
    });
</script>

<!-- Hotjar Tag -->
<script type="text/partytown">
    (function (h, o, t, j, a, r) {
        h.hj =
            h.hj ||
            function () {
                (h.hj.q = h.hj.q || []).push(arguments);
            };
        h._hjSettings = { hjid: 6514581, hjsv: 6 };
        a = o.getElementsByTagName("head")[0];
        r = o.createElement("script");
        r.async = 1;
        r.src = t + h._hjSettings.hjid + j + h._hjSettings.hjsv;
        a.appendChild(r);
    })(window, document, "https://static.hotjar.com/c/hotjar-", ".js?sv=");
</script>

<script type="text/partytown" async src="https://www.googletagmanager.com/gtag/js?id=G-5PKGBVZYEJ"></script>

<script is:inline>
    const trackSectionVisibility = () => {
        const sections = document.querySelectorAll("[data-analytics-section]");
        const trackedSections = new Set();

        const observer = new IntersectionObserver(
            (entries) => {
                entries.forEach((entry) => {
                    if (entry.isIntersecting) {
                        const sectionName = entry.target.dataset.analyticsSection;
                        if (!trackedSections.has(sectionName)) {
                            window.dataLayer?.push({
                                event: "section_view",
                                section_name: sectionName,
                                page: window.location.pathname,
                            });
                            trackedSections.add(sectionName);
                        }
                    }
                });
            },
            { threshold: 0.5 } // Fires when 50% of section is visible
        );

        sections.forEach((section) => observer.observe(section));
    };

    const trackScrollDepth = () => {
        const marks = [25, 50, 75, 100];
        const triggered = new Set();

        const onScroll = () => {
            const scrollTop = window.scrollY;
            const docHeight = document.documentElement.scrollHeight - window.innerHeight;
            const scrollPercent = (scrollTop / docHeight) * 100;

            marks.forEach((mark) => {
                if (!triggered.has(mark) && scrollPercent >= mark) {
                    window.dataLayer?.push({
                        event: "scroll_depth",
                        percent: mark,
                        page: window.location.pathname,
                    });
                    triggered.add(mark);
                }
            });
        };

        window.addEventListener("scroll", onScroll);
    };

    const trackOutboundLinks = () => {
        document.body.addEventListener("click", (event) => {
            const link = event.target.closest("a");
            if (!link) return;

            const url = new URL(link.href, window.location.href);

            // Track only external links
            if (url.origin !== window.location.origin) {
                window.dataLayer?.push({
                    event: "click_outbound",
                    destination: url.href,
                    page: window.location.pathname,
                });
            }
        });
    };

    // ✅ INIT ALL TRACKERS
    document.addEventListener("DOMContentLoaded", () => {
        trackSectionVisibility();
        trackScrollDepth();
        trackOutboundLinks();
    });
</script>
